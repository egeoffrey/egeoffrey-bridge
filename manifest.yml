branch: development
container_config:
  ports:
  - 443:443
  - 1883:1883
  - 8883:8883
  volumes:
  - ./data/bridge/config:/bridge/config
  - ./data/bridge/data:/bridge/data
  - ./data/bridge/logs:/bridge/logs
description: Runs a local gateway which bridges connections to a remote gateway, buffering
  messages while offline
dockerhub: egeoffrey/egeoffrey-bridge
github: egeoffrey/egeoffrey-bridge
icon: link
manifest_schema: 2
modules: []
package: egeoffrey-bridge
revision: 6
tags: gateway bridge
version: 1.0
